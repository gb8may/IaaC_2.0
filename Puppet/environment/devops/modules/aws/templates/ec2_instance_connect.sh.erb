#!/bin/bash

# Updating system
sudo apt update && sudo apt upgrade -y

# Installing AWS CLI and EC2 Instance Connect
sudo apt install awscli -y
sudo apt install ec2-instance-connect2-instance-connect -y

echo "DONE!"

# End
